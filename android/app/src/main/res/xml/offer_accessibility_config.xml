<?xml version="1.0" encoding="utf-8"?>
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFlags="flagRetrieveInteractiveWindows|flagReportViewIds"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100"
    android:packageNames="com.ubercab.driver,com.uber.eats.driver,com.doordash.driverapp,com.taxis99.driver"
    />
<!--
  packageNames: ajuste conforme os pacotes reais instalados no device de teste.
  Descobrir o package name real:
    adb shell pm list packages | grep -i uber
    adb shell pm list packages | grep -i 99

  notificationTimeout baixo (100ms) porque a oferta do Uber tem poucos segundos
  na tela antes de expirar — precisa reagir rápido.
-->
